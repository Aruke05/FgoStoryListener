FGO 剧情文本监听器 v2.6.10

批量策略调整
- 完整剧情预载由每批最多 80 条改为每批 100 条。
- 例如当前阶段有 129 条时，将按 100 + 29 两批完成，而不是 80 + 49。
- 该策略只作用于进入剧情后的完整脚本预翻译。
- 当前正在播放的实时台词仍具有最高优先级。
- 普通历史补译在 app-server 模式下仍采用保守小批量，避免当前句被大批历史记录阻塞。

设置界面
- “每批条数”更名为“普通补译每批条数”，避免与完整剧情固定 100 条/批混淆。
- 可配置上限同步提高到 100。
