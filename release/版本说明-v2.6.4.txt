FGO 剧情文本监听器 v2.6.4

修复：
- 修复软件与 FGO 同时启动时偶发的 "unable to find module 'libil2cpp.so'"。
- Hook 现在会等待 libil2cpp.so、IL2CPP domain 与 Assembly-CSharp.dll 完成加载，并每 300ms 自动重试。
- 等待期间显示正常的“正在等待 FGO Unity 运行时加载”状态，不再立即显示红色错误；最多等待约 120 秒后才报告真正的超时。

其余：
- 保留 v2.6.3 的人物名日中双语显示、关卡筛选、完整游戏 LOG 同步、实时监听、整段预翻译与历史数据库。
- 不使用截图或 OCR。

临时处理旧版：
- 若 v2.6.3 已出现该错误，可等待 FGO 完全进入游戏后点击“重新连接”。建议直接升级到 v2.6.4。
