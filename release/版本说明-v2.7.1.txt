FGO 剧情文本监听器 v2.7.1

排版修复
- 修复当前译文窗口把根窗口宽度误当成文本内容宽度，导致中文译文按过宽尺寸排版后被右侧裁切。
- 日文与中文现在使用内容卡片的实际可用宽度换行。
- 默认窗口高度由 285 调整为 380，最小内容高度根据实际换行结果动态增长。
- 中文字号从过重的 15 调整为更适合长句阅读的 13，仍保留明确的译文层级。
- 新增布局回归测试，禁止重新使用错误的根窗口宽度估算。

功能保持
- 完整剧情仍按每 100 条一次请求翻译。
- 当前译文与双语 LOG 的“解释这句”和解释缓存保持不变。
- 所有窗口仍不使用持久置顶或强制抢焦点。
