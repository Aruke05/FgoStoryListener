FGO 剧情文本监听器 v2.9.8

修复 Codex 自动发现误选旧 CLI

- 自动发现 PATH 以及 Windows Codex 桌面版安装目录中的候选 CLI，执行 --version 后按语义版本选择正常运行的新版。
- 不再无条件优先 codex.cmd；不写死桌面版的哈希安装路径。用户显式配置路径仍优先，不自动覆盖。
- 探测结果按安装文件时间与大小缓存，避免每个翻译批次重复启动探测；文件变化或安装新版本后重新选择。
- 保留 v2.9.7 的动态模型目录、推理强度、Fast 能力查询和用户选择保护。
- 不修改 Codex 全局配置，不向模拟器发送操作。

验证
- 86 项单元/回归测试通过。
- 不指定 CLI 路径，实际自动选择本机 0.154.0-alpha.6.2，模型目录包含 gpt-6-astra。
- 设置自动刷新、保留用户选择、推理强度更新、刷新中关闭窗口烟测通过。
- 使用自动发现的 CLI，显式 gpt-6-astra 的真实 codex exec 翻译返回“你好。”。

使用与磁盘占用
- 关闭旧监听器，启动 dist-v2.9.8/FgoStoryListener/FgoStoryListener.exe。
- codex 路径留空即可自动选择；若原来手动指定了旧 CLI，需要清空该字段并保存。
- 按用户明确要求，新包完成验证后删除旧安装目录、旧发布压缩包及回滚备份，只保留最新版本；不删除剧情数据库、原始日文、源码和工具依赖。
- 本机安装版本会变化，上述版本是本次实测记录，不是程序内置限制。
