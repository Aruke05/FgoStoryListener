FGO 剧情文本监听器 v2.6.5

已修复：
1. 修复跨阶段时游戏 LOG 的排序锚点仍停留在上一阶段，导致新阶段台词被插到上一阶段末尾选项之前、悬浮译文看起来卡住的问题。
2. 当前译文不再用“该关卡排序后的最后一行”猜测，而是精确定位游戏 LOG 最新一页。
3. 修复完整脚本预翻译把动画/等待参数误当台词的问题：
   - 按 executeMessageFlagList 划分消息区块；
   - 只读取空 executeTagList 对应的可见文本；
   - 将同一页的多行合并后再预翻译；
   - 排除 time 2.0、I 0.1、A normal、人物资源名等非台词数据。
4. 启动时仅清理 v2.6.3-v2.6.4 产生的隐藏错误预载行，不删除已经显示的剧情历史；清理前自动创建 history-before-v2.6.5.db。
5. 保留 libil2cpp.so 启动等待与自动重试。

验证：
- hook.js 已通过 Node 语法检查。
- 40 项 Python 自动测试通过。
- 新增跨阶段顺序回归测试。

说明：
预翻译功能本身是支持的。v2.6.4 的问题发生在“从 IL2CPP 解析数组重建完整台词页”这一步，不是 Codex 模型未工作。
